from setuptools import setup

setup(name='tg_distributions',
      version='0.3',
      description='Gaussian distributions',
      packages=['tg_distributions'],
      author = 'Tushar Garg',
      zip_safe=False)
