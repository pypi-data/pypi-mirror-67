from setuptools import setup

setup(name='zains_gaussian_binomial_distributions',
      version='1.0.0',
      description='Gaussian & Binomial distributions',
      packages=['zains_gaussian_binomial_distributions'],
      zip_safe=False)
