from setuptools import setup

setup(name='gauss_bin_distrib',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['gauss_bin_distrib'],
      zip_safe=False)
