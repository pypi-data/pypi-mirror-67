#!/usr/bin/python3 -u
# BOTLIB - Framework to program bots.
#
# bin/botctl (run command on the system bot)

import lo
import os
import sys

from bot.krn import Kernel
from lo.shl import execute

def main():
    lo.workdir = "/var/lib/botd"
    k = Kernel()
    mods = k.walk("bot")
    k.scan(mods)
    k.cmd(" ".join(sys.argv[1:]))
    
execute(main)
os._exit(0)
