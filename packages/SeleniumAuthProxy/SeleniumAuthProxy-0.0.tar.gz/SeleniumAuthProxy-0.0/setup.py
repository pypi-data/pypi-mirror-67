from setuptools import setup

setup(name='SeleniumAuthProxy',
      version='0.0',
      description='Library for installing in Selenium proxy servers with authorization',
      packages=['SeleniumAuthProxy'],
      author_email='mart-iden@rambler.ru',
      zip_safe=False)