from setuptools import setup

setup(name='gdabd_distributions',
      version='0.2',
      description='Gaussian & Binomial distributions',
      packages=['gdabd_distributions'],
      zip_safe=False)
