from setuptools import setup

setup(name='distributions_doppiavi',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['distributions_doppiavi'],
      zip_safe=False)
