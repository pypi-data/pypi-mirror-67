from setuptools import setup

setup(name='jenny_udacity_distributions',
      version='0.1',
      description='Gaussian and Bionomial distributions',
      packages=['jenny_udacity_distributions'],
      zip_safe=False)
