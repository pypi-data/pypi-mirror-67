#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['lint_diffs']

package_data = \
{'': ['*']}

install_requires = \
['unidiff>=0.6.0']

entry_points = \
{'console_scripts': ['lint-diffs = lint_diffs:main']}

setup(name='lint_diffs',
      version='0.1.22',
      description='Runs one or more linters, but only outputs errors if diffs have lint issues.',
      author='Atakama, LLC',
      author_email='dev-support@atakama.com',
      url='https://github.com/atakamallc/lint-diffs',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      entry_points=entry_points,
      python_requires='>=3.6',
     )
