[tool.poetry]
name = "update-docker-detach-keys"
version = "0.1.2"
description = ""
authors = ["Eyal Levin <eyalev@gmail.com>"]
readme = "README.md"

[tool.poetry.scripts]
update-docker-detach-keys = "update_docker_detach_keys.cli:cli"

[tool.poetry.dependencies]
python = "^3.6"
click = "^7.1.2"
json-file = "^0.1.6"

[tool.poetry.dev-dependencies]
pytest = "^5.2"
black = {version = "^19.10b0", allow-prereleases = true}

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
